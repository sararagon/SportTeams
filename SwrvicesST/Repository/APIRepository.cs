using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Text;
using ModelsST;
using Newtonsoft.Json;

namespace ServicesST.Repository
{
   /* public class APIRepository : IRepository
    {
        
            using (var player = new HttpPlayer())
            {
                HttpResponseMessage response = c.GetAsync("https://webbasketapi.azurewebsites.net/api/jugadores").Result;
                string contenido = response.Content.ReadAsStringAsync().Result;
                {
                    List<Usuario> lista;
                    lista = JsonConvert.DeserializeObject<List<Usuario>>(contenido);
                    return lista;

                }
            }


        
    }*/
}
